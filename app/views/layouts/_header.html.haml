%header
  .title_header イジゲンシステム管理
  - if logged_in?
    .header_content=link_to "#{current_user.name}", user_path(current_user), class: "header_fonts"
    .header_content=link_to "ログアウト", logout_path, method: "delete", class: "header_fonts"
